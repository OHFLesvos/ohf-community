import { isoDateFormat, dateFormat, dateWeekdayFormat, dateTimeFormat, timeFormat, timeFromNow } from "@/utils/date";
export default {
    methods: {
        isoDateFormat,
        dateFormat,
        dateWeekdayFormat,
        dateTimeFormat,
        timeFormat,
        timeFromNow
    }
};
