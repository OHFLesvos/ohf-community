import { dateFormat, dateTimeFormat, timeFormat, timeFromNow } from "@/utils/date";
export default {
    methods: {
        dateFormat,
        dateTimeFormat,
        timeFormat,
        timeFromNow
    }
};
